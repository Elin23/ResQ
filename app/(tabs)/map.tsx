import { View } from 'react-native';
import AppText from '../../src/components/AppText';

export default function MapScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <AppText>الخريطة، قريباً</AppText>
    </View>
  );
}